# 2026-03-31 · Consejo arriba, Equipo debajo y PCs por Terminal

AdmiraNext Team

## Trabajo realizado

- Reordenada la vista de `AdmiraNext Control` para que el bloque `Consejo de Administracion` quede arriba y el bloque `Equipo` justo debajo.
- Eliminado el render mezclado por filas sueltas y sustituido por bloques agrupados estables.
- En equipos Windows el selector por defecto pasa a `Terminal`, evitando que el envio intente ir a `Claude` o `Codex` por defecto.
- Ajustado el canal Windows para que `terminal` no se confunda con ventanas de `Codex`.
- Verificado un envio real del PC local `PC Sitges 3 Monitores` con respuesta `ok: true`.
- Subida la version visible a `v2.3.3`.
- Subida la version del paquete a `0.3.3`.
