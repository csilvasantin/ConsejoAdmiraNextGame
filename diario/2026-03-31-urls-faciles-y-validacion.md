# 2026-03-31 · URLs faciles y validacion en navegador

AdmiraNext Team

## Trabajo realizado

- Añadidas URLs faciles de recordar para local y publico.
- Alias principales:
  - `control` -> `teamwork.html`
  - `equipo` -> `index.html`
  - `admin` -> `consejo.html`
- En local quedan activas como rutas amigables del servidor.
- En GitHub Pages quedan activas como paginas de redireccion simples.
- Subida la version visible a `v2.3.1`.
- Subida la version del paquete a `0.3.1`.

## Regla operativa

- Cada actualizacion debe:
  - subir version;
  - publicarse;
  - abrirse en navegador;
  - quedar lista para validacion visual del usuario.
